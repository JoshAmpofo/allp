#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str1[80];

	strcpy(str1, "\"Programming is like building a multilingual puzzle\n");
	puts(str1);

	return (0);
}
